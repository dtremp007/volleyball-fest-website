/** Reference data that rarely changes. */
export const REFERENCE_DATA_STALE_TIME = 30 * 60 * 1000;

/** Public season context — invalidate on season state mutations. */
export const PUBLIC_CONTEXT_STALE_TIME = 2 * 60 * 1000;
